<UserControl x:Class="DapperClassMate.UI.Controls.IntegerSpinEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="28">
    <Border BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
            BorderThickness="1"
            CornerRadius="0"
            Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="ValueTextBox"
                     Grid.Column="0"
                     BorderThickness="0"
                     VerticalAlignment="Stretch"
                     VerticalContentAlignment="Center"
                     Padding="4,0,2,0"
                     Background="Transparent"
                     TextChanged="ValueTextBox_TextChanged"
                     PreviewKeyDown="ValueTextBox_PreviewKeyDown"
                     LostFocus="ValueTextBox_LostFocus" />

            <StackPanel Grid.Column="1"
                        Orientation="Vertical">
                <RepeatButton x:Name="UpButton"
                              Height="14"
                              BorderThickness="1,0,0,0.5"
                              BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                              Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                              Padding="0"
                              Focusable="False"
                              Interval="80"
                              Delay="400"
                              Click="UpButton_Click">
                    <Path Data="M 0,4 L 5,0 L 10,4"
                          Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                          StrokeThickness="1.2"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Width="10"
                          Height="5" />
                </RepeatButton>

                <RepeatButton x:Name="DownButton"
                              Height="14"
                              BorderThickness="1,0.5,0,0"
                              BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                              Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                              Padding="0"
                              Focusable="False"
                              Interval="80"
                              Delay="400"
                              Click="DownButton_Click">
                    <Path Data="M 0,0 L 5,4 L 10,0"
                          Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                          StrokeThickness="1.2"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Width="10"
                          Height="5" />
                </RepeatButton>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>